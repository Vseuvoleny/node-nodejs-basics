# User Management CRUD API

RESTful API для управления пользователями, реализованная на Node.js с TypeScript. Приложение предоставляет полный набор CRUD операций с валидацией данных и обработкой ошибок.

## 🚀 Быстрый старт

```bash
npm install          # Установка зависимостей
npm run start:dev    # Запуск в режиме разработки
npm run start:build  # Сборка проекта
npm run start:multi  # Запуск с кластеризацей
npm run test         # Запуск тестов
npm dev              # Запуск production версии
```

Для работы приложения нужно создать в корне файл .env/.env.development и указать PORT:

```env
PORT=4000
```

Моковые данные содержатся в файле ./src/mock/users.json и имеют структуру:

```json
{
  "data": [
    {
      "id": "38efa18f-6b36-496d-862e-af8f1310cbe2",
      "name": "Jonh",
      "age": 30,
      "hobbies": []
    }
  ]
}
```
